# Copy to Credentials.env (gitignored) and fill in real values.
SSH_HOST=
SSH_USER=root
SSH_PORT=22

# Preferred: use a key.
SSH_KEY_PATH=

# Fallback only — avoid storing passwords on disk.
SSH_PASSWORD=

REMOTE_PATH=/var/www/sasha-nikitin/beta
